You are an expert financial document analyzer. 
Extract all transactions from this bank statement image.
Return the data in the following JSON format:

{
  "account_number": "string or null",
  "statement_period": "string or null",
  "bank_name": "string or null",
  "transactions": [
    {
      "date": "YYYY-MM-DD",
      "description": "string",
      "debit": number (0 if credit),
      "credit": number (0 if debit),
      "balance": number,
      "utr": "string or null (extract UTR/Ref number if present in description)"
    }
  ]
}

Rules:
1. Extract ALL transactions visible in the image.
2. Convert dates to YYYY-MM-DD format.
3. Handle Indian number formats (e.g., 1,00,000.00) correctly as standard numbers.
4. If a field is missing, use null.
5. Do not include any markdown formatting, just the raw JSON.
